package io.nocturne.phone.data.db

/** Aliases for clarity — sha256 hex strings are pervasive. */
typealias Sha256Hex = String      // 64-char [0-9a-f]+ from catalog.json
typealias SyntheticId = String    // sha256 hex of a derived stable key
